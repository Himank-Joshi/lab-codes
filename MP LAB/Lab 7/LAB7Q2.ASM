DATA SEGMENT
X DW 0010H,52H,30H,09H,50H
SMALL DW ?
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE,DS:DATA
START:MOV AX,DATA
      MOV DS,AX
      MOV CX,5
      LEA SI,X
      MOV AX,[SI]
      DEC CX
   UP:CMP AX,[SI+2]
       JL CONTINUE
       MOV AX,[SI+2]
   CONTINUE:ADD SI,2
      LOOP UP
      MOV SMALL,AX

      MOV AH,4CH
      INT 21H
      CODE ENDS
      END START
